# Viggle-Animate accelerator profiles

This folder is reserved for accelerator profiles validated for Viggle-Animate.
The model already includes its own distillation LoRA for three-step sampling.
Standard MiniMax H3 accelerator profiles are kept in `profiles/minimax_h3/`.
